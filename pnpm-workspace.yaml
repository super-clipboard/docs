onlyBuiltDependencies:
  - esbuild

# Exempt our own packages from pnpm v11's default 24h supply-chain quarantine
# so freshly published releases can be consumed by CI immediately.
minimumReleaseAgeExclude:
  - '@super-clipboard/*'

allowBuilds:
  '@parcel/watcher': true
